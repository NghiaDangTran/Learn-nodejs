const num1 = 5
const num2 = 10

function addvalues() {
    console.log(`the sum is : ${num1+num2}`)
}

addvalues()
    // if we used this in orhter fileit will still print oputbut we cannot use any value